1) тип данных следует выбрать  long long double
2) получится x=2
3) остаток от деления 0
4) в х в выражении х += cos((x=1)-(x=1)) будет записано 2.
5) х после выполнения кода if (x=0) х=1 будет = 1.
6)
7)  массив float x[20] займет 80 байт памяти. float занимает 4 байта если  4*20 получится 80 байт.
8) чтобы сохранить телефонный справочник мобильных номеров белорусов, нужно 388 мегабайт памяти.
9) не больше 2 -3 секунды.
10)
11) другие фуннкции можно вызывать в функции main.
12) причина в том что программа использует алгоритм двоичного поиска.
13) если при попытки сохранения данных в файл была выявлена ошибка, программа выведет сообщение об ошибке на экран.
14) увеличить массив, так как не хватает места в памяти.
